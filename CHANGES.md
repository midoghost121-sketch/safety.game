# تعديلات نظام لعبة مفتش السلامة

## ملخص التغييرات الرئيسية

تم إعادة تصميم نظام التقييم في اللعبة لتبسيط الآليات والتركيز على الإجراءات التصحيحية الصحيحة.

---

## 1. إزالة مصفوفة المخاطر من نافذة الخطر

### ما تم إزالته:
- دالة `buildMatrixGrid()` من نافذة اكتشاف الخطر (تبقى مصفوفة المخاطر المستقلة في القائمة الرئيسية)
- الحقل `selectedMatrixCell` من الاستجواب
- التقييم على أساس تقدير مستوى الخطورة

### السبب:
تبسيط عملية التقييم والتركيز على الإجراء التصحيحي الصحيح فقط

---

## 2. إضافة نظام اختيار المهمات (PPE) عند دخول الموقع

### عناصر جديدة:

#### أ) نافذة اختيار المهمات:
```html
<div id="ppeModal" class="modal hidden">
    <!-- اختيار معدات الوقاية المطلوبة للموقع -->
</div>
```

#### ب) دوال جديدة:
1. **`showPPESelectionModal(section)`**: عرض نافذة اختيار المهمات عند دخول موقع جديد
   - تعرض قائمة بجميع معدات الوقاية المتاحة
   - تميز المهمات المطلوبة بإطار مقطوع (dashed border)

2. **`confirmPPESelection()`**: تأكيد اختيار المهمات
   - حساب النقاط بناءً على:
     - المهمات الصحيحة المختارة: 50 نقطة
     - المهمات الناقصة: 25 نقطة
   - عرض تقييم فوري

3. **`showPPEResult(score, feedback, isSuccess, section)`**: عرض نتيجة اختيار المهمات
   - تقييم بسيط: صحيح ✅ / ناقص ⚠️

#### ج) متغيرات جديدة:
```javascript
let currentSection = null;                    // الموقع الحالي
let selectedPPE = [];                         // المهمات المختارة
let sectionPPEShown = {};                    // تتبع الأقسام التي تم فتح النافذة فيها
```

---

## 3. تعديل بيانات الأقسام

### إضافة خاصية `requiredPPE` لكل قسم:

```javascript
const sections = [
    { 
        id: 'intake', 
        name: 'مأخذ مياه نهر النيل', 
        // ...
        requiredPPE: ['helmet', 'slip_shoes', 'life_vest', 'harness', 'safety_shoes']
    },
    // ... باقي الأقسام
];
```

---

## 4. تعديل نافذة اكتشاف الخطر

### قبل:
- اختيار مستوى الخطورة من مصفوفة
- اختيار معدات الوقاية الشخصية
- اختيار الإجراء التصحيحي

### بعد:
- **اختيار الإجراء التصحيحي فقط**

### التبسيط:
```javascript
// الآن فقط:
if (selectedCorrective === currentHazard.correctiveAction) {
    score = 100; // إجابة صحيحة
} else {
    score = 0;   // إجابة خاطئة
}
```

---

## 5. تعديل دالة `updateCurrentSection()`

### السلوك الجديد:
عند دخول موقع جديد:
1. التحقق من الموقع الحالي
2. إذا كان موقعاً جديداً:
   - تشغيل صوت الموقع
   - **فتح نافذة اختيار المهمات (أول مرة فقط)**

```javascript
if (!sectionPPEShown[found.id] && gameState.running && !gameState.paused) {
    showPPESelectionModal(found);
    sectionPPEShown[found.id] = true;
}
```

---

## 6. نظام التقييم الجديد

### نقاط تقييم اختيار المهمات:
- ✅ اختيار صحيح كامل: 50 نقطة
- ⚠️ اختيار ناقص: 25 نقطة

### نقاط تقييم الإجراء التصحيحي:
- ✅ إجابة صحيحة: 100 نقطة
- ❌ إجابة خاطئة: 0 نقطة

---

## 7. تحديثات الواجهة

### عناصر جديدة في HTML:
- `#ppeModal`: نافذة اختيار المهمات
- `#sectionNamePPE`: عنوان الموقع
- `#ppeSelectGrid`: شبكة اختيار المهمات
- `#btnConfirmPPE`: زر تأكيد الاختيار

### استيلات CSS جديدة:
```css
.ppe-item.required {
    border-color: #ff9800;
    border-style: dashed;
    opacity: 0.7;
}
```

---

## 8. تحديثات المستمعين للأحداث

```javascript
// زر تأكيد المهمات
document.getElementById('btnConfirmPPE')
    .addEventListener('click', confirmPPESelection);
```

---

## 9. سلوك اللعبة الجديد

### تسلسل اللعب:
1. **دخول موقع جديد**
   ↓
2. **فتح نافذة اختيار المهمات**
   ↓
3. **اختيار المهمات وتأكيد**
   ↓
4. **عرض نتيجة المهمات**
   ↓
5. **البحث عن المخاطر**
   ↓
6. **عند اكتشاف خطر: اختيار الإجراء التصحيحي**
   ↓
7. **عرض النتيجة**

---

## 10. ملاحظات تقنية

### التوافقية:
- ✅ جميع المتغيرات القديمة محفوظة
- ✅ لا تأثير على نظام الصوت
- ✅ لا تأثير على الخريطة والرسومات

### الأداء:
- ✅ لا تكاليف أداء إضافية
- ✅ تخزين مؤقت للنوافذ المفتوحة

---

## التعديلات القادمة المحتملة

1. إضافة مستويات صعوبة
2. تصنيفات أداء مفصلة
3. تقارير شاملة بعد انتهاء اللعبة
4. نظام تقدم محسّن

